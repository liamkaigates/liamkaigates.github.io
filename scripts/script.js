function toggleMenu() {
    var menu = document.querySelector('.mobile-nav-links');
    menu.classList.toggle('open');
}

function toggleProjectPopup() {
    var body = document.querySelector('body');
    body.classList.toggle('project-popup-open');
}
